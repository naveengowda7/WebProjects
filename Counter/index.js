
import React from 'react'; //to rendor react js components
import ReactDOM from 'react-dom/client'; // to load this into the web-browser
import App from './App';


const root = ReactDOM.createRoot(document.getElementById("root")); //it tells react to control the element
root.render(
    <App />

);

